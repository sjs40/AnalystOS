# Evaluation

Unit tests exercise input validation, deterministic question priority, lineage, and contradiction preservation. Integration tests should mock SEC/network calls and verify source caching. Evaluation fixtures must label synthetic data and test: fact versus inference, preserved contradictions, willingness to remain unresolved, and the required claim → evidence → source lineage.
