# Workflows

The Phase 1 source-evidence workflow is: retrieve or import → immutable source → normalize → extract observations → propose classified claims → attach directional evidence → create and prioritize questions.

The SLM Grad PLUS seed workflow is planned for Phase 2 because it requires the thesis primitives (hypotheses, revisions, conditions, scenarios) specified for that phase. The kernel already supports the evidence portion without inventing a conclusion.
